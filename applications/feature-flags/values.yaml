# This file is empty because all default values for the subcharts
# (api and mongodb) are already correctly configured
# in their respective values.yaml files.
#
# You only need to add configuration here if you want to OVERRIDE
# the defaults of the subcharts.
#
# Example override:
# api:
#   replicaCount: 5
