apiVersion: v1
kind: ConfigMap
metadata:
  name: feature-flags-grafana-dashboards
  namespace: kps
  labels:
    grafana_dashboard: "1"
data:
  feature-flags-overview.json:
    {{ .Files.Get "grafana-dash.json" | nindent 4 }}